user nginx;
worker_processes auto;

error_log /var/log/nginx/error.log notice;
pid       /var/run/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  /var/log/nginx/access.log  main;

    sendfile        on;
    keepalive_timeout  65;

    upstream app_cluster {
        # Round-robin routing to multiple node instances
        server app-node-1:3000;
        server app-node-2:3000;
        
        # IP-hash can be used to bind a client to a server instance,
        # but with Redis Pub/Sub, any server can handle any client in any room.
        # ip_hash;
    }

    server {
        listen 80;
        server_name localhost;

        # WebSocket connection handshake path
        location /connect/ {
            proxy_pass http://app_cluster;
            proxy_http_version 1.1;
            
            # Enable WebSocket upgrade protocol headers
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "Upgrade";
            
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            # Extended read/write timeouts for persistent websocket connections
            proxy_read_timeout 3600s;
            proxy_send_timeout 3600s;
        }

        # Regular HTTP REST APIs & health checks
        location / {
            proxy_pass http://app_cluster;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
}
